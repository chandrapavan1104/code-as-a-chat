# ── Phase 1: Server core + SysMon + File Manager ──────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0
psutil>=6.0.0

# ── Phase 4: Telegram V1 client ───────────────────────────────────────────────
python-telegram-bot>=21.0
httpx>=0.27.0

# ── Push notifications: FCM HTTP v1 to the Gajala app (optional) ───────────────
google-auth>=2.30.0
requests>=2.31.0

# ── Phase 3: Codex skill via Open Interpreter (uncomment when ready) ──────────
# open-interpreter>=0.3.0

# ── Phase 4: PII masking — V2 (uncomment when ready) ─────────────────────────
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0
