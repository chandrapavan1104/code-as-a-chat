<?xml version="1.0" encoding="utf-8"?>
<!-- Gajala quick-actions widget: Lock (network) · Ask · Dump (deep-links). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_card_bg"
    android:padding="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gajala"
        android:textColor="#2EA6FF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:paddingBottom="8dp"
        android:letterSpacing="0.02" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <!-- Lock -->
        <LinearLayout
            android:id="@+id/tile_lock"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginEnd="6dp"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/widget_tile_bg"
            android:padding="6dp">
            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_widget_lock"
                android:contentDescription="Lock Mac" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Lock"
                android:textColor="#E9EDF0"
                android:textSize="11sp"
                android:paddingTop="4dp" />
        </LinearLayout>

        <!-- Ask -->
        <LinearLayout
            android:id="@+id/tile_ask"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginEnd="6dp"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/widget_tile_bg"
            android:padding="6dp">
            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_widget_chat"
                android:contentDescription="Ask Gajala" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Ask"
                android:textColor="#E9EDF0"
                android:textSize="11sp"
                android:paddingTop="4dp" />
        </LinearLayout>

        <!-- Brain dump -->
        <LinearLayout
            android:id="@+id/tile_dump"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/widget_tile_bg"
            android:padding="6dp">
            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_widget_brain"
                android:contentDescription="Brain dump" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Dump"
                android:textColor="#E9EDF0"
                android:textSize="11sp"
                android:paddingTop="4dp" />
        </LinearLayout>
    </LinearLayout>

    <TextView
        android:id="@+id/lock_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tap Lock to sleep the Mac"
        android:textColor="#8A99A8"
        android:textSize="10sp"
        android:paddingTop="8dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
